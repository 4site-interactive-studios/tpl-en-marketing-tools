{
  "name": "ngs-en-marketing-tools",
  "version": "1.0.0",
  "description": "A template for building EN marketing tools projects using MJML.",
  "scripts": {
    "build": "mjml ./src/*.mjml -o ./dist/ --config.allowIncludes=true",
    "watch": "mjml -w ./src/*.mjml -o ./dist/ --config.allowIncludes=true"
  },
  "keywords": [],
  "author": "4Site Interactive Studios",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "mjml": "^5.2.2"
  }
}
